import styled from "styled-components";

export const WordsListStyled = styled.div`
  max-width: 700px;
  height: 210px;

  position: relative;

  margin: 0 auto;
  overflow: hidden;

  display: flex;
  flex-wrap: wrap;
  gap: 5px 15px;
`;
